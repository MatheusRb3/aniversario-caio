window.addEventListener('DOMContentLoaded', () => {
      const convite = document.getElementById('container');
      setTimeout(() => {
        convite.classList.add('visible');
      }, 300);
    });